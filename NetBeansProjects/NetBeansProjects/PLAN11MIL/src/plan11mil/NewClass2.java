/*
Declare una variable estatica del tiopo string, diga al usuario que ingrese alli su nombre completo cree tres merodos:
a) que covierta a mayusc la primera letra de cada comp del nombree, y el resto a minuscula y lo devuelva como un string.
b)el segundo que tome la primera letra de cada componente de (nombre y apellido)  y lo devuelva como un unico string por es decir las iniciales.
c) que cuente las palabras contenidas en el nombre completo y devuelva esa cant como un int.
Los metodos no deben recibir parametros ya que podran usar sin problemas la variable por ser de clase.
El metodo main debe pedir el nombre completo y utilizar los metodos creados mostrando los valores recibidos.
 */
package plan11mil;

/**
 *
 * @author Windows
 */
public class NewClass2 {
    
}
